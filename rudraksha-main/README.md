
## Run Locally

**Prerequisites:**  Node.js

1. Install dependencies:
   `npm install`
2. Copy `.env.example` to `.env.local` and add your API key:
   `cp .env.example .env.local`
   Then edit `.env.local` to set your `GEMINI_API_KEY`.
3. Run the app:
   `npm run dev`
